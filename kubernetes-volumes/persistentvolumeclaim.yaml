apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kuber-pvc
  namespace: homework
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi